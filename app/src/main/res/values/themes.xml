<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base XML theme; Compose (MaterialTheme) drives the actual colors at runtime. -->
    <style name="Theme.FlipIQ" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/flip_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
